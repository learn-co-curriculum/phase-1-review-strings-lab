/*const currentUser = 'Grace Hopper';

const welcomeMessage = `Welcome to Flatbook, ${currentUser}!`;

const excitedWelcomeMessage = welcomeMessage.toUpperCase();

const shortGreeting = `Welcome, ${currentUser.slice(0,1)} !`;
*/

//quick syntax review from memory


var currentUser = 'Grace Hopper';

let welcomeMessage = `Welcome to Flatbook, ${currentUser}!`;

const excitedWelcomeMessage = welcomeMessage.toUpperCase();

//var shortGreeting = `Welcome, ${currentUser.slice(0, 1)}!`;

var secondUser = `easdsg`;

var capSecondUser = secondUser.toUpperCase();

var shortGreeting = `Welcome, ${capSecondUser.slice(5, 6)}!`
